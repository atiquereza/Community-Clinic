@model IEnumerable<treatment.Models.Medicine>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.MedicineName)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.WeightContaints)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.MG_ML)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Quantity)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.MedicineName)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.WeightContaints)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.MG_ML)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Quantity)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.MedicineID }) |
            @Html.ActionLink("Details", "Details", new { id=item.MedicineID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.MedicineID })
        </td>
    </tr>
}

</table>
